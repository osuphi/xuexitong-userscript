# 安全策略

## 这个脚本会碰什么、不会碰什么

**不会做**：

- 不读取、不保存、不外发账号凭据（Cookie、密码、token 都不碰）；
- **任何配置下都不发起外部网络请求**（脚本已不含任何自动作答/大模型调用）；
- 不写入 `localStorage`，诊断信息只存在内存中；
- 不上传任何文件。

**唯一的外部资源**：当页面本身没有 jQuery 时，脚本会补加载一次 jQuery CDN
（`https://code.jquery.com/jquery-3.6.0.min.js`）。除此之外没有其它外链，包括不加载、不连接任何"AI 答题"服务。

> 油猴元数据里保留了 `@grant GM_xmlhttpRequest`，这只是为了让脚本继续运行在油猴沙箱中
> （去掉全部 `@grant` 会改为注入页面上下文，可能撞上页面 CSP 或覆盖页面全局变量），
> 脚本内**没有任何** `fetch` / `XMLHttpRequest` 调用 —— CI 的 `tests/redlines.mjs` 会静态校验这一点。

## 隐私注意事项

诊断报告会包含课程页面标题、学习步骤名和控制台日志（可能含课程内容片段）。
页面地址与视频地址已自动去掉 `?query`，避免带出临时票据；分享前仍建议自己过一眼。
报告里不含任何账号信息。

## 报告安全问题

本仓库是**私有仓库**。如果发现安全相关问题（例如：意外发起网络请求、注入行为影响页面其它功能），请：

1. 优先通过仓库 Issue 反馈（附上诊断报告与复现步骤）；
2. 内容敏感时不要公开贴细节，直接联系仓库所有者。

修复会记录在 `CHANGELOG.md`。

## 已知的非安全问题（不必报告）

- 画面倍速、任务点是否计入学习时长由**服务端**判定，脚本无法绕过 —— 这是平台约束，不是缺陷；
- 页面自身若报 `$(...).getNiceScroll is not a function` 一类插件错误，先看诊断报告里的 `jQuery 来源` 字段：
  若为「页面自带」，说明与本脚本无关。
