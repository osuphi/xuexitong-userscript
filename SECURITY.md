# 安全策略

## 这个脚本会碰什么、不会碰什么

**不会做**：

- 不读取、不保存、不外发账号凭据（Cookie、密码、token 都不碰）；
- 默认配置下**零外部网络请求**；
- 不写入 `localStorage`，诊断与密钥都只在内存中；
- 不上传任何文件。

**会做的**：

- 唯一的外部请求来源是页面原本就依赖的 jQuery CDN（仅当页面缺 jQuery 时补加载）；
- 若你**显式开启** LLM 功能（默认关闭）并填写密钥，脚本才会把「题干 + 选项」文本发到
  `llmEndpoint`（默认 `https://opencode.ai/zen/go/v1/chat/completions`），
  请求头带 `Authorization: Bearer <你的密钥>`。
  密钥只保存在当前页面内存里，刷新即失效，**不会被写进仓库或日志**；诊断报告里只记录"是否已配置"这一布尔值。

## 密钥与隐私注意事项

- 不要把密钥粘到 issue、PR、聊天记录或截图里。
- 诊断报告会包含课程页面标题、学习步骤名和控制台日志（可能含课程内容片段）。
  页面地址与视频地址已自动去掉 `?query`，避免带出临时票据，但分享前仍建议自己过一眼。
- LLM 功能会向第三方端点发送题目文本，请自行确认符合你所在课程与平台的规定。

## 报告安全问题

本仓库是**私有仓库**，如果你发现了安全相关的问题（例如：可能泄露凭据、意外发起网络请求、
注入行为影响页面其他功能），请：

1. 优先通过仓库的 Issue 反馈（附上诊断报告与复现步骤）；
2. 如果内容敏感，不要在公开渠道贴出细节，直接联系仓库所有者。

我们会尽快确认，并在 `CHANGELOG.md` 里记录修复版本。

## 已知的非安全问题（不必报告）

- 画面倍速、任务点是否计入学习时长由**服务端**判定，脚本无法绕过 —— 这是平台约束，不是缺陷；
- 页面自身若报 `$(...).getNiceScroll is not a function` 一类的插件错误，先看诊断报告里的
  `jQuery 来源` 字段：若为「页面自带」，说明与本脚本无关；若为「本脚本注入的 CDN 版」，
  则是 F18 那类问题，请反馈。
